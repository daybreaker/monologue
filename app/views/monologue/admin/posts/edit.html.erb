<h1><%= t(".header") %> "<%= @post.title %>"</h1>
<%= monologue_admin_form_for ["admin", @post], method: :put, multipart: true do |post| %>
  <%= render partial: 'form', locals: {post: post} %>
<% end %>
